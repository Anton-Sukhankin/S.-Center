# Переносимый контракт прокрутки и фиксации шахматки

**Статус:** нормативный технологически нейтральный контракт целевого frontend-поведения  
**Текущий источник evidence:** browser-прототип; точный статус подтверждения вынесен в [контрольную точку соответствия](./prototype-verification.md)  
**Целевая зрелость:** переносимая реализация в production frontend; пакет не устанавливает backend-, инфраструктурные, security- или эксплуатационные гарантии головной системы  
**Основной читатель:** coding agent, которому поручена реализация функциональности в целевом frontend-проекте  
**Граница:** первоначальная и восстановленная вертикальная позиция матрицы, inline nested scroll, изолированная полноэкранная прокрутка и согласованная фиксация областей раскрытой работы

## 1. Назначение пакета

Пакет описывает наблюдаемый результат, семантические области интерфейса, браузерные инварианты и программу приёмки. Он не задаёт framework, компонентную библиотеку, DOM-дерево, CSS-селекторы, имена классов, API компонентов, state management или структуру исходных файлов целевого проекта.

Разработчик передаёт эту директорию coding agent вместе с доступом к целевому репозиторию и его штатными командами запуска и проверки. Изучение исходного прототипа S.Center, транскрипций, временного плана корректировок и внутренней документации workspace для downstream-реализации не требуется.

## 2. Состав и владельцы сведений

| Документ | Состояние | Владеет |
|---|---|---|
| [interface-roles-and-relations.md](./interface-roles-and-relations.md) | Нормативное | Семантические роли видимых областей, пространственные отношения и владельцы осей прокрутки |
| [behavior-and-algorithm.md](./behavior-and-algorithm.md) | Нормативное | Требуемое поведение, браузерные инварианты, эталонная алгоритмическая модель и известные ошибки |
| [acceptance-scenarios.md](./acceptance-scenarios.md) | Нормативное | Детерминированные сценарии проверки требований |
| [agent-task.md](./agent-task.md) | Операционное | Порядок применения пакета в неизвестной заранее frontend-архитектуре; новых требований не создаёт |
| [prototype-verification.md](./prototype-verification.md) | Текущий audit checkpoint, ненормативное | Сопоставление контракта с прототипом, автоматизированное покрытие, команды и ограничения evidence |
| [prototype-kinematics-reference.md](./prototype-kinematics-reference.md) | Текущий reference profile, ненормативное | Точные числовые параметры, фазы и формулы исходного прототипа для режима максимальной близости |
| [prototype-kinematics-vectors.json](./prototype-kinematics-vectors.json) | Текущее машиночитаемое evidence, ненормативное | Исполняемые примеры кинематики и синхронизация reference-параметров с JS/CSS прототипа |

Полное правило хранится только у указанного владельца. Остальные документы используют идентификатор и ссылку, а не пересказывают правило как новый источник. `prototype-verification.md` не создаёт требований: при расхождении его нужно обновить по коду и проверкам либо зафиксировать неподтверждённый target, но не менять нормативный контракт молча.

## 3. Порядок использования

1. Прочитать [роли и отношения](./interface-roles-and-relations.md), не интерпретируя концептуальную схему как обязательный DOM.
2. Прочитать [поведение и алгоритмическую модель](./behavior-and-algorithm.md).
3. Прочитать [сценарии приёмки](./acceptance-scenarios.md) и определить поддерживаемые целевым проектом desktop-браузеры.
4. Исследовать целевой репозиторий и самостоятельно сопоставить роли `UI-Rxx` с фактическими компонентами и прокручиваемыми элементами.
5. Реализовать контракт средствами и в границах целевого проекта.
6. Выполнить все применимые сценарии во всех поддерживаемых проектом desktop-браузерах.
7. Передать отчёт о сопоставлении ролей, изменениях, проверках и ограничениях. Дописывать этот пакет на стороне целевого проекта не требуется.

[Контрольная точка прототипа](./prototype-verification.md) объясняет происхождение и текущую глубину проверки. Downstream-агент использует её как evidence и пример команды, но подтверждает результат заново в целевом репозитории. Если поручение требует максимально близко воспроизвести ощущение текущего прототипа, дополнительно применяются [reference-профиль](./prototype-kinematics-reference.md) и [JSON-векторы](./prototype-kinematics-vectors.json); без такого условия они не становятся обязательными параметрами целевой реализации.

Готовая формулировка поручения находится в [agent-task.md](./agent-task.md).

## 4. Нормативный язык

- **ДОЛЖЕН / НЕ ДОЛЖЕН** — обязательное условие приёмки.
- **СЛЕДУЕТ** — проверенная рекомендация web-платформы; допустимо другое решение, если все связанные требования и сценарии проходят без деградации.
- **МОЖЕТ** — разрешённый вариант, не создающий отдельного требования.
- Идентификаторы `UI-Rxx` обозначают семантические роли, а не DOM `id`.
- Идентификаторы `SCROLL-*`, `SMOOTH-*`, `STICKY-*`, `WEB-*` и `LIFE-*` обозначают требования.
- Идентификаторы `AC-*` обозначают сценарии приёмки.

## 5. Что остаётся на усмотрение целевого проекта

Целевой агент самостоятельно выбирает:

- состав компонентов и обёрток;
- способ получения ссылок на реальные scroll-контейнеры;
- место хранения краткоживущего состояния анимации;
- конкретную easing-функцию и длительность, если движение остаётся плавным и проходит приёмку;
- способ автоматизации браузерных сценариев;
- локальную архитектуру адаптации корпоративных компонентов.

Если готовый компонент скрывает необходимый scroll-элемент или уже владеет конфликтующим поведением, агент сначала ищет предусмотренный публичный API, затем продуктовую обёртку. Изменение внутренностей общей библиотеки не предполагается без отдельного разрешения владельца целевого проекта.

В режиме максимальной близости этот выбор начинается с параметров [эталонного профиля кинематики](./prototype-kinematics-reference.md). Их адаптация допускается только с фиксацией причины и browser-evidence того, что нормативный результат сохранён.

## 6. Осознанные ограничения

- Контракт относится к desktop-web вводу, который браузер предоставляет как `WheelEvent`, включая совместимые тачпад-жесты. Touch/pointer-прокрутка не специфицирована и не должна изобретаться как требование.
- Программная передача остатка `UI-R01` относится только к inline-композиции. Полноэкранная поверхность является изолированным scroll-контекстом: `UI-R10` не передаёт остаток скрытому списку работ.
- Контракт не определяет данные, редактирование ячеек, визуальную палитру статусов и бизнес-расчёты готовности.
- Числа видимых строк и условие прекращения фиксации являются продуктовыми инвариантами. Текущие пиксельные размеры строк и константы easing не являются нормативными требованиями; они вынесены в отдельный ненормативный reference-профиль для необязательного режима точного воспроизведения.
- Внешние изображения не являются обязательным контекстом: логическая карта и схема движения встроены в Markdown. Каталог `assets/` появится только для проверенного визуального состояния, которое нельзя однозначно восстановить по тексту.

## 7. Когда пакет пересматривается

Пакет проверяется целиком, если изменяется хотя бы одно из условий:

- количество видимых строк или правило прекращения фиксации;
- состав фиксируемых областей;
- владелец вертикальной или горизонтальной прокрутки;
- порядок передачи движения на границе;
- поддерживаемые типы ввода;
- наблюдаемые требования к плавности;
- правило первоначальной или восстановленной позиции матрицы;
- геометрия внешнего header или вкладок, влияющая на верхнюю границу фиксации.

Изменение точных current-констант или порядка фаз без изменения наблюдаемого target требует синхронно обновить reference-профиль, JSON-векторы и checkpoint, но не переписывать нормативные требования.

Внутренний рефакторинг реализации без изменения наблюдаемого контракта не требует переписывать пакет.
